# Production Environment Configuration
# For Electron desktop app: uses /api/v1 which is proxied by server.cjs to the backend
# For Web deployment: configure this to point directly to your backend server
VITE_API_BASE=/api/v1
